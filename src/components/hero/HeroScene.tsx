"use client";

// Home hero — warm, low-poly paper-craft desk scene (Phase 1 spec, Canva
// concept: laptop/plant/mug/lamp, unfolds like a pop-up book on load, idles
// with small charm loops after). Mounts/unmounts with the Home route only —
// NOT a cross-route persistent canvas (see AmbientLayer for the sitewide
// ambient layer, which is separate).
//
// STUB — real react-three-fiber scene (materials/lighting, cursor parallax,
// scroll-exit) lands in the hero build stream. Intentionally inert (returns
// null) so it is safe to import/mount ahead of that work without any visual
// or bundle-size effect.
export default function HeroScene() {
  return null;
}
