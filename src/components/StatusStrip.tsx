// Stub. Ambient status strip (atmosphere layer, aria-live="polite",
// prefers-reduced-motion static fallback per the spec) lands in a later
// stream.
export default function StatusStrip() {
  return null;
}
