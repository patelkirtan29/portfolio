// Stub. Real persistent nav (Home / Work / About / Contact, plain-English
// labels, optional 00/01/02/03 index decoration per the spec) lands in a
// later stream.
export default function Nav() {
  return <nav />;
}
